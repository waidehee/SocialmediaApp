<div class="container">
  <div class="row">
    <div class="col4">
      <div class="media">
  <img src="{{user.image}}" class="img-thumbnail">
  <div class="media-body">
    <h5 class="mt-0">{{user.fullname}}</h5>
    <p>{{user.email}}</p>
  </div>
</div>
    </div>
  </div>
</div>